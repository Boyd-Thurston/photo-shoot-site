import React from "react";

import NavBar from "../components/common/NavBar";

/**Styless component controling the layout of the about page.*/
const About = () => {
  return (
    <div>
      <NavBar currentTab="about" />
    </div>
  );
};

export default About;
